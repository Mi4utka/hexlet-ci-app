name : hexlet
on: push
jobs: 
    build: 
     runs-on: ubuntu-latest
     steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-node@v3
      - run: npm install
      - run: make lint
      - run: make test
        name: snyk
      - uses: snyk/actions/node@master
        env:
          SNYK_TOKEN: ${{ 16c4dabb-4dc4-4dba-bbd1-02efd53501d5 }}
        with:
          args: --severity-threshold=high

